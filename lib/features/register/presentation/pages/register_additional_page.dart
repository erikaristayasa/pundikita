import 'package:flutter/material.dart';

class RegisterAdditionalPage extends StatelessWidget {
  const RegisterAdditionalPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
